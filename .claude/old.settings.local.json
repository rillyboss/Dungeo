{
  "hooks": {
    "Stop": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "bash $CLAUDE_PROJECT_DIR/scripts/hooks/session-complete-reminder.sh"
          }
        ]
      }
    ]
  },
  "description": "Local settings for TestRPGGame - won't be committed",
  "permissions": {
    "allow": [
      "Bash(python -m json.tool:*)"
    ]
  }
}
